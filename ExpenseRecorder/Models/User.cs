using Microsoft.AspNetCore.Identity ;

namespace ExpenseRecorder.Models ;
// TODO: lists of categories and transactions ? For ef core configuration ? But how to load them ?
public class User : IdentityUser
{ }
